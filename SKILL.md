---
name: rnd_proposal
description: |
  한국 정부 R&D 과제계획서, 연구개발계획서, RFP 기반 제안서, 공모사업 사업계획서를
  multi-step orchestration으로 작성하는 스킬. 단일 거대 프롬프트로 한 번에 본문을
  뽑지 않고 Input Analyzer → RFP Analyzer → Form Analyzer → Requirement Analyzer →
  RFP-Form Filling Strategy Interview → Requirement Interview Gate → Writing Guide
  Builder → Strategy Planner → Section Writer → Reviewer → Visual Asset Planner →
  Export Agent 순으로 단계별 산출물을 누적해 작성한다. 연차별 목표·컨소시엄 역할·
  정량 KPI·실증 범위·인체적용시험·인허가·개별인정·매출 같은 위험 표현은 임의 확정
  하지 않고 선택형 인터뷰로 사용자 결정을 받는다.

  사용자가 "사업조서", "과제 계획서", "연구개발계획서", "R&D 제안서", "정부과제
  신청서", "연구계획서", "제안서 작성", "사업계획서", "RFP 분석", "공모사업 계획서",
  "평가위원 리뷰", "KPI 표", "역할 분담표", "연차별 개발 계획", "도식 명세",
  "Mermaid 명세", "PPT 변환용 구조", "HWPX/DOCX 양식 기준본"을 언급하거나,
  RFP/공고문/지원사업 양식(.hwpx, .hwp, .pdf, .docx) 파일과 함께 정부 R&D 과제
  계획서·제안서 작성을 의뢰할 때, 또는 KIAT/KEIT/IITP/NRF/NIPA/중기부 등 R&D
  전문기관 공모사업과 관련된 작성을 요청할 때 반드시 이 스킬을 사용할 것.
  사용자가 "스킬"이라는 단어를 명시적으로 쓰지 않아도 위 키워드/맥락이 보이면
  자동 적용한다. 단순 일반 Word 문서 작성에는 사용하지 말 것 (그건 docx 영역).
  HWPX 파일 직접 분석·편집이 필요하면 hwpxskill을 함께 사용한다.
---

# rnd_proposal

정부 R&D 계획서를 "그럴듯한 문장 묶음"이 아니라 RFP 요구사항, 제출 양식, 평가항목, 기술개발 흐름, 기관 역할, 정량 KPI, 실증·검증 전략이 맞물린 계획 구조로 설계한다.

## Core Rule

단일 거대 프롬프트로 한 번에 작성하지 않는다. 반드시 역할 기반 단계로 나누고, 각 단계의 구조화된 결과를 다음 단계 입력으로 사용한다.

계획서 "작성" 요청이라도 바로 본문을 쓰지 않는다. RFP와 제출양식은 외부 제약이므로, 먼저 분석한 뒤 "이 RFP와 양식을 어떤 전략과 내용으로 채울지"를 사용자에게 확인한다. 질문은 너무 딥한 기술 설계까지 들어가기 전, 작성 방향·강조점·범위·역할·KPI 수준을 정하는 데 집중한다. 연차별 목표·컨소시엄 역할·정량 KPI·실증 범위가 불명확하면 `Requirement Interview Gate`를 반드시 수행한다.

처음 시작할 때는 바로 파일 분석이나 질문으로 들어가지 말고, 사용자에게 진행 방식의 전체 흐름을 짧게 설명한 뒤 시작한다. 설명은 5~7줄 이내로 유지하고, 각 단계가 왜 필요한지 한 문장 수준으로만 말한다. 예:

```text
진행 방식은 다음 순서입니다.
1. 입력자료 확인: RFP, 제출양식, 참고자료, 참여기관 정보를 확인합니다.
2. 요구사항 분석: RFP의 필수 요구, 평가항목, 위험 표현을 추출합니다.
3. 양식 매핑: 제출양식의 항목별로 어떤 내용을 채워야 하는지 연결합니다.
4. 작성전략 인터뷰: 선택이 필요한 쟁점만 보기 기반으로 확인합니다.
5. 초안 작성·검토: 확정 답변과 가정을 분리해 본문을 작성하고 누락을 점검합니다.
```

권장 실행 순서:

1. Input Analyzer
2. RFP Analyzer
3. Form Analyzer
4. Requirement Analyzer
5. RFP-Form Filling Strategy Interview
6. Requirement Interview Gate
7. Writing Guide Builder
8. Strategy Planner
9. Section Writer
10. Reviewer
11. Visual Asset Planner
12. Export Agent

파일 기반 작업이면 현재 작업공간에 `proposal_work_<short-topic>/`를 만들고 중간 산출물을 남긴다. 원본 파일은 수정하지 않고 `00_inputs/`에 복사본을 둔다.

## When Starting

먼저 사용자가 준 RFP, 공고문, 제출 양식, 참고자료, 기존 제안서, 평가항목, 참여기관 정보를 확인한다. 자료가 부족하더라도 모든 값을 임의 확정하지 않는다. 질문은 일반적인 질문 목록을 던지는 방식이 아니라, `RFP Analyzer`와 `Form Analyzer` 결과를 근거로 만들어야 한다.

다음 순서를 지킨다.

1. RFP 요구사항을 추출한다.
2. 제출 양식의 목차와 각 항목이 요구하는 작성 내용을 추출한다.
3. RFP 요구사항과 양식 항목을 매핑한다.
4. 매핑 결과를 바탕으로 "각 양식 항목을 어떤 방향으로 채울지"를 묻는 작성전략 질문을 만든다.
5. 비어 있는 정보, 선택지가 갈리는 전략, 사용자의 의도 확인이 필요한 지점을 찾는다.
6. 그 지점만 질문한다.

아래 항목은 자료에 없으면 초안 작성 전에 사용자에게 질문한다.

- 1차년도 목표와 수행 범위
- 2차년도 목표와 최종 실증 수준
- 주관기관·공동기관·수요기관 역할
- 기관별 핵심 산출물 소유 또는 책임 범위
- 정량 KPI 중 확정값이 필요한 항목
- 인체적용시험, 인증, 인허가, 사업화 매출처럼 책임·위험이 큰 범위

그 밖의 단순 배경정보나 보조 수치는 `가정`, `기입 필요`, `보완 필요정보`로 분리해 진행할 수 있다.

입력 분석 결과는 다음처럼 구조화한다.

```yaml
input_inventory:
  rfp:
  submission_form:
  references:
  previous_proposals:
  evaluation_criteria:
  participant_info:
  missing_inputs:
  assumptions:
```

## Intermediate Schemas

각 단계는 자유 서술만으로 끝내지 않는다. 중간 결과는 YAML 또는 Markdown 표를 포함한 구조화 형식으로 저장하거나 응답한다.

상세 스키마가 필요하면 `references/intermediate_schemas.md`를 읽는다.

## Analyzer Responsibilities

RFP Analyzer는 사업 목적, 핵심 키워드, 필수 기술, 데이터 요구사항, 실증 요구사항, 정량 목표, 성과·산출물 요구사항, 필수 포함 키워드, 제외·주의사항을 추출한다.

Form Analyzer는 제출 양식의 목차, 항목별 작성 목적, 요구 내용, 표·도식 필요 여부, 평가항목 연결 위치, 분량 특성을 분석한다.

Requirement Analyzer는 기능, 시스템, 데이터, AI 모델, 검증, KPI, 산출물, 실증, 역할 분리 요구사항을 정리한다.

## RFP-Form Filling Strategy Interview

RFP와 양식은 사용자가 만든 문서가 아니라 제출처가 요구하는 틀이다. 따라서 분석 후 바로 본문을 채우지 말고, 사용자가 그 틀을 어떻게 채우고 싶은지 확인한다.

이 단계의 질문은 너무 깊은 구현 세부사항을 묻지 않는다. 목적은 작성 방향을 정하는 것이다.

질문 방식은 가능한 한 `선택지 기반 순차 인터뷰`로 진행한다. 한 번에 긴 질문 목록을 던지지 말고, 작성 방향을 결정하는 질문을 1개씩 제시한 뒤 사용자의 답을 반영하여 다음 질문으로 넘어간다. 각 질문에는 2~4개의 대표 선택지를 제공하고, 항상 `기타/직접입력` 가능성을 열어 둔다. 사용자가 선택지만으로 답하기 어려운 경우 직접 입력한 내용을 우선 반영한다.

Claude Code 환경에서는 `AskUserQuestion` 도구가 선택형 사용자 입력 UI를 제공한다. 이 도구가 사용 가능하면 텍스트 번호 목록 대신 `AskUserQuestion`을 최우선으로 사용한다. 사용자는 키보드 방향키 또는 클라이언트가 제공하는 선택 동작으로 보기를 고를 수 있다. 도구가 없거나 현재 모드에서 사용할 수 없으면 텍스트 번호 선택 방식으로 폴백한다.

선택형 UI(`AskUserQuestion`)를 사용할 때의 작성 규칙:

- 질문은 한 번에 1개만 보낸다 (또는 관련 묶음 최대 4개까지).
- 선택지는 2~4개를 기본으로 하고, 너무 많은 선택지가 필요하면 후속 질문으로 나눈다.
- 추천 선택지가 있으면 첫 번째에 두고 라벨에 `(추천)` 또는 `(Recommended)`를 붙인다.
- `AskUserQuestion`은 자동으로 `Other`(직접입력) 선택지를 추가하므로, 중복해서 `기타/직접입력` 선택지를 만들지 않는다.
- 사용자가 직접 입력한 답변은 선택지보다 우선한다.

UI 도구가 없으면 텍스트로 다음 형식처럼 묻는다.

```text
질문: 사업화 제품의 중심을 무엇으로 둘까요?
1) B2B 기능성 평가 플랫폼
2) 후보소재 검증·자료패키지 서비스
3) 플랫폼 + 후보소재 제품화 균형형
4) 기타/직접입력: 원하는 방향을 적어주세요
```

질문은 사용자의 이전 답변을 반영하여 다음 질문의 선택지를 좁히거나 표현을 바꾼다. 예를 들어 사용자가 `플랫폼 + 제품화 균형형`을 선택하면 다음 질문은 플랫폼 기능과 제품화 범위의 균형을 묻고, `후보소재 제품화`를 선택하면 후보소재, 인체적용시험, 개별인정 책임 범위를 우선 묻는다.

질문 깊이 원칙:

- 묻는다: 강조 전략, 사업화 포지션, 연차별 큰 목표, 실증 범위, 컨소시엄 역할 경계, KPI 수위, 제외할 범위
- 묻지 않는다: 모델 아키텍처 세부 파라미터, DB 컬럼 단위 설계, 실험 프로토콜 상세조건, 예산 세목 단위 배분
- 단, 사용자가 상세 작성을 요청했거나 양식이 상세값을 요구하면 후속 질문으로 분리한다.

양식 항목별로 최소 다음 질문 관점을 적용한다.

```yaml
filling_strategy_questions:
  - form_section: "사업 개요/추진배경"
    ask: "이 항목에서 정책 적합성, 시장 문제, 기술 병목 중 무엇을 가장 앞세울지"
  - form_section: "사업화 실증 대상기술"
    ask: "대상기술을 플랫폼, 검증서비스, 자료패키지, 현장실증 중 무엇으로 정의할지"
  - form_section: "실증 내용/실증참여체계"
    ask: "실증을 실제 현장 적용까지 쓸지, 예비검증·PoC 수준으로 쓸지"
  - form_section: "1차년도 목표 및 내용"
    ask: "1차년도는 설계·구축·베타·예비검증 중 어디까지 쓸지"
  - form_section: "2차년도 목표 및 내용"
    ask: "2차년도는 고도화·심화검증·자료패키지·유료 PoC 중 무엇으로 끝낼지"
  - form_section: "사업화 추진계획"
    ask: "매출, LOI, 유료 PoC, 투자, 고용 중 어떤 성과를 전면에 둘지"
```

출력은 먼저 `질문`이어야 한다. 사용자가 "질문 없이 가정해서 써"라고 명시하지 않았다면, 이 단계에서 초안 본문을 작성하지 않는다.

## Requirement Interview Gate

Requirement Analyzer 이후, 다음 중 하나라도 불명확하면 본문 작성 전에 질문한다. 질문은 반드시 RFP·양식 분석 결과와 연결되어야 하며, "그냥 궁금한 것"이나 일반 체크리스트가 아니어야 한다.

- 연차별 목표가 `설계/개발/예비검증/현장실증/사업화` 중 어디까지인지 불명확함
- 컨소시엄 기관명 또는 역할 경계가 불명확함
- KPI 수치가 확정값처럼 쓰일 위험이 있음
- 실증 대상, 후보 소재, 수요기업, 검증 범위가 불명확함
- "개별인정", "인체적용시험", "인허가", "매출" 등 책임 범위가 큰 표현을 써야 함

질문은 짧게 하되, 반드시 의사결정을 끌어내는 형태로 한다. 질문이 많으면 한 번에 최대 7개까지만 묻고, 나머지는 `후속 확인`으로 남긴다.

기본은 `순차 선택형 질문`이다. 특히 연차별 목표, 기관 역할, KPI 수위, 실증 범위, 인체적용시험·인허가·개별인정·매출 같은 위험 표현은 사용자가 선택지 중 하나를 고르거나 직접 입력하도록 한다. 선택지가 있는 질문은 아래 원칙을 따른다.

- `AskUserQuestion` 같은 선택형 사용자 입력 UI가 사용 가능하면 텍스트 번호 목록보다 우선 사용한다.
- 선택형 UI를 사용할 때는 위 `RFP-Form Filling Strategy Interview`의 UI 작성 규칙을 동일하게 적용한다.
- 추천 선택지를 먼저 둔다.
- 선택지는 서로 겹치지 않게 만든다.
- 마지막에는 항상 `기타/직접입력`을 둔다(`AskUserQuestion`을 쓸 때는 자동 제공되므로 중복 추가 금지).
- 사용자가 번호만 답해도 해석 가능하게 만든다.
- 사용자가 직접 입력한 답변은 선택지보다 우선한다.
- 질문을 한 번에 모두 던지기보다, 답변에 따라 다음 질문을 이어간다.

예:

```text
질문: 2차년도 인체적용시험 범위는 어디까지로 쓸까요?
1) IRB 프로토콜·시험기관 연계 준비까지
2) 소규모 인체적용시험 실제 수행
3) 본 과제 범위에서 제외
4) 기타/직접입력
```

각 질문은 다음 형식을 따른다.

```yaml
question:
  linked_rfp_requirement:
  linked_form_section:
  why_needed:
  question:
  answer_options_or_expected_input:
    - recommended_option:
    - other_options:
    - 기타_직접입력: true
  impact_if_unknown:
```

예를 들어 양식에 `1차년도 목표 및 내용`이 있고 RFP에 `현장 적용 및 Pilot-Test`가 있으면, 질문은 "1차년도에 Pilot-Test를 실제 수행할지, 아니면 실증 설계와 예비검증까지 둘지"처럼 작성한다. 양식에 `실증참여체계 및 절차`가 있으면, 질문은 "주관기관·공동기관·수요기관의 실제 명칭과 역할 경계"를 물어야 한다.

기본 질문 후보는 다음과 같지만, 실제 질문은 RFP·양식 매핑 결과에 맞게 바꿔야 한다.

```text
1. 과제 중심은 무엇인가: 플랫폼 개발 / 기능성 검증·자료 패키지 / 현장 실증·사업화 / 균형형?
2. 1차년도는 어디까지 수행하는가: 설계 / 베타 플랫폼 / 후보 소재 예비평가 / in vitro 예비검증 / 현장 적용?
3. 2차년도 종료 시 반드시 남아야 하는 결과는 무엇인가: 실증 플랫폼 / 심화검증 / 자료 패키지 / 유료 PoC·계약?
4. 주관기관과 공동기관별 역할은 어떻게 나누는가?
5. 인체적용시험·인허가·개별인정 신청은 직접 수행인가, 제출 준비자료 수준인가?
6. 확정 가능한 KPI와 아직 쓰면 안 되는 KPI는 무엇인가?
```

사용자가 "일단 가정해서 진행"하라고 명시한 경우에만 임시안을 작성한다. 이때도 `가정안`, `확정 필요`, `위험 표현`을 별도로 표시한다.

## RFP-Form Mapping

본문 작성 전 다음 매핑을 반드시 만든다.

```yaml
rfp_form_mapping:
  - rfp_requirement:
    form_section:
    needed_user_decision:
    missing_information:
    draft_risk_if_assumed:
    question_to_user:
```

이 매핑에서 `needed_user_decision` 또는 `missing_information`이 비어 있지 않은 항목은 질문 후보가 된다. 단순 사실 확인은 `기입 필요`로 남길 수 있지만, 작성 방향이 달라지는 항목은 반드시 질문한다.

## Writing Guide

참고자료가 없으면 기본 작성가이드를 사용한다.

- 정부 R&D 문체 사용
- 평가위원 친화적 표현 사용
- 정량 표현 우선
- 과장 표현 최소화
- 실증 및 검증 중심
- 기관 역할 명확화
- 기술 흐름 중심 설명
- AI 기술은 데이터, 검증, 서비스와 연결

참고자료가 있으면 내용 복사가 아니라 작성 규칙만 추출한다. 문체, 논리 흐름, 자주 쓰는 표현, 표·도식 스타일, 정량목표 표현 방식, 평가 대응 문장 패턴, 기술 서술 방식을 분석한다.

상세 문체 규칙은 `references/writing_rules.md`를 읽는다.

## Strategy Planner

본문 작성 전에 핵심 개발 방향, 차별화 포인트, 강조 키워드, 평가 대응 전략, 우리기관 담당 범위, 제외 범위, 실증 전략, 활용 및 사업화 방향을 고정한다.

역할 경계가 불명확하면 임의로 확정하지 말고 후보안과 확인 필요사항을 분리한다.

## Section Writer

Section Writer는 RFP 원문을 직접 다시 해석하지 않는다. 입력은 반드시 RFP Analyzer 결과, Requirement Analyzer 결과, Strategy Planner 결과, Writing Guide 결과로 제한한다.

연구개발 내용은 표 중심으로 쓰지 않는다. 기본 출력은 문장 기반 설명형 문단이다. 특히 `1차년도 목표 및 내용`, `2차년도 목표 및 내용`, `연구개발 내용 및 범위`, `추진전략 및 체계`는 표만으로 작성하면 실패로 본다.

`개발내용 및 범위`를 작성할 때 양식이 `[주관연구개발기관]`, `[공동연구개발기관1]`처럼 기관별 기입란을 제공하면 반드시 기관별로 나눈다. 단, 기관명 아래에 단순 bullet만 쓰지 말고, 각 기관 아래에 `공공 DB 수집 및 파이프라인 구축`, `AI 후보소재 평가모델 개발`, `in vitro 검증체계 구축`, `제품화·자료패키지 설계`처럼 기관이 책임지는 대제목형 세부과업을 두고, 각 세부과업 아래에 목적, 세부 개발내용, 입력 데이터, 구현 방법, 타 기관 인터페이스, 검증 방법, 산출물을 충분한 문단으로 작성한다. 연차별 `개발내용 및 범위`가 몇 개 문단과 표 한 개 수준이면 실패로 보고 확장 작성한다.

반드시 다음 흐름이 자연스럽게 연결되게 쓴다.

```text
왜 필요한가 -> 무엇을 개발하는가 -> 어떤 데이터를 사용하는가 -> 어떤 기술을 적용하는가 -> 어떤 방식으로 구현하는가 -> 어떻게 검증하는가 -> 어떤 결과물이 생성되는가
```

기술 설명은 문장 중심, 관리 정보는 표 중심으로 작성한다. 표는 연차별 개발 일정, KPI, 역할 분담, 산출물 목록, 비교표, 정량 목표에 제한적으로 사용한다.

연차별 목표 및 내용은 각 연차마다 최소 다음 문단을 포함한다.

1. 해당 연차의 필요성과 위치
2. 연차 목표
3. 주요 개발 내용
4. 사용할 데이터·기술·시스템
5. 기관별 역할과 인터페이스
6. 실증·검증 방법
7. 산출물과 다음 연차 또는 사업화 연결

표를 넣더라도 위 문단을 대체할 수 없다.

특히 `(3) 1차년도 개발내용 및 범위`, `(3) 2차년도 개발내용 및 범위` 같은 양식 항목은 각 연차마다 기관별 소제목을 먼저 두고, 기관별로 2~4개 세부 대제목을 만든다. 세부 대제목 번호 형식은 양식의 기존 표기법을 따른다. 예를 들어 양식이 `1), 2), 3)`을 쓰면 그 형식을 쓰고, `가), 나), 다)`, `(1), (2), (3)`, `①, ②, ③`, bullet을 쓰는 양식이면 해당 형식을 유지한다. 중요한 것은 번호 형식이 아니라 기관별 책임 과업이 대제목 수준으로 구분되고, 각 대제목별로 2~4개 이상의 실질 문단이 작성되는 것이다. 역할 표는 기관별 본문 이후 요약으로만 둔다. 각 세부 대제목 본문 아래에는 해당 과업을 설명하는 이미지/도식 삽입 필요성을 검토한다. 예: `공공 DB 수집 및 AI 학습용 데이터 파이프라인 구축` 아래에는 데이터 수집-정제-매핑-학습셋 구성 파이프라인 도식을 넣으면 좋다. 그림이 필요하다고 판단되면 해당 세부과업 본문 바로 아래에 Visual Asset 마커를 남긴다.

상세 작성 원칙은 `references/section_writer_rules.md`를 읽는다.

## Reviewer

생성 이후 Reviewer를 반드시 수행한다. 평가항목 대응성, 정량 KPI, 역할 중복, 과장 표현, RFP 누락, 실증 가능성, 정부과제 문체, 기술 흐름, 표 중심 과다, 추상 표현을 점검한다.

상세 체크리스트는 `references/reviewer_checklist.md`를 읽고 결과를 `충족 / 보완 필요 / 기입 필요`로 정리한다.

## Visual Asset Planner

Visual Asset Planner는 대표 도식 2~3개만 만드는 단계가 아니다. 본문을 단락 또는 소절 단위로 훑고, 평가자가 글만으로 이해하기 어려운 부분마다 필요한 그림을 제안한다. 그림은 꼭 도식일 필요가 없다.

다음 위치는 반드시 시각자료 필요성을 검토한다.

- 사업화 실증 대상기술
- 실증 내용
- 실증참여체계 및 절차
- 최종목표 및 평가방법
- 1차년도 목표 및 내용의 세부 꼭지
- 2차년도 목표 및 내용의 세부 꼭지
- 개발내용 및 범위의 기관별 세부 대제목 각각
- 추진전략 및 체계
- 사업화 추진계획
- 연구개발성과 활용방안 및 기대효과

예를 들어 1차년도 연구내용 중 `플랫폼 개발` 아래에 `AI agent 기반 후보 소재 평가` 꼭지가 있으면, 그 단락 아래에 AI agent 동작 흐름, 사용자 질의 화면 예시, 데이터-에이전트-검증기관 연결 그림 중 하나를 제안해야 한다.

시각자료 유형:

- process_diagram: 개발·검증·실증 흐름
- system_architecture: 플랫폼, DB, AI 모델, API, 보안 구조
- agent_interaction: 사용자, AI agent, 도구, 데이터, 리포트 간 상호작용
- screen_mockup: 플랫폼 화면 또는 리포트 예시
- data_example: 입력 데이터, 처리 결과, 스코어링 예시
- validation_result_mock: 검증결과 리포트 또는 대시보드 예시
- role_map: 컨소시엄 역할과 인터페이스
- roadmap: 연차별 개발·실증 일정
- kpi_tree: 목표, KPI, 측정방법, 증빙자료 구조
- scenario_illustration: 수요기업 활용 시나리오

각 시각자료는 다음처럼 본문 위치와 연결한다.

```yaml
visual_asset_spec:
  id:
  linked_section:
  linked_paragraph_or_topic:
  asset_type:
  title:
  purpose:
  key_message:
  why_needed:
  components:
  suggested_format: diagram | mockup | table_visual | infographic | screenshot_style | generated_image
  render_candidate: Mermaid | SVG | PPT | Figma | draw.io | imagegen | manual
  notes_for_renderer:
```

Visual Asset Spec을 별도 파일로만 만들지 않는다. Section Writer 또는 Export Agent는 본문 드래프트의 해당 위치에 반드시 삽입 마커를 남긴다. 마커는 그림이 들어갈 정확한 문단 아래에 배치한다.

본문 삽입 마커 형식:

```markdown
[그림 삽입: VA-001 | 제목: AI agent 기반 후보 소재 평가 흐름 | 유형: agent_interaction | 목적: 사용자 질의가 데이터 검색, 후보 스코어링, 검증 프로토콜 추천, 리포트 생성으로 이어지는 흐름 설명]
```

마커 규칙:

- `VA-001` 같은 ID는 `visual_asset_spec.id`와 반드시 일치해야 한다.
- 마커는 장·절 끝에 몰아서 넣지 말고, 관련 단락 바로 아래에 넣는다.
- 표나 문단이 그림을 대체하지 않는다. 그림이 필요하다고 판단한 경우 본문에 위치 표시를 남긴다.
- 최종 Export Agent는 `visual_asset_plan`과 본문 내 삽입 마커를 대조해 누락된 그림 위치가 없는지 확인한다.
- 아직 렌더링하지 않은 경우에도 마커는 남긴다.

기본 절차:

```text
본문 단락별 그림 필요성 스캔 -> Visual Asset Spec 생성 -> 본문 드래프트에 삽입 마커 배치 -> 사용자 검토 -> 도구별 렌더링
```

렌더링 후보는 Mermaid, ReactFlow, SVG Generator, Figma, draw.io, PPT Diagram Generator, MCP image generation tool(`mcp__openai-image__generate_image`)이다. 상세 포맷은 `references/diagram_spec.md`를 읽는다.

## Export Agent

요청에 따라 다음 산출물을 만든다.

- 정부과제 계획서 초안
- 항목별 상세 작성 내용
- KPI 표
- 역할 분담표
- 연차별 개발 계획
- 실증 계획
- 발표자료용 도식 명세
- PPT 변환용 구조 데이터
- Mermaid 또는 Diagram Spec
- HWPX/DOCX 양식 복붙용 Markdown 기준본

HWPX 파일 직접 분석·편집이 필요하면 `hwpxskill` 스킬을 함께 사용한다.

## Questions

질문은 필요한 경우에만 짧게 한다. 다만 계획서 작성에서 연차별 목표, 기관 역할, 실증 범위, 예산·매출·정량목표를 확정값으로 써야 하는 경우는 "필요한 경우"가 아니라 필수 질문 대상이다. 원본 파일 덮어쓰기처럼 되돌리기 어려운 작업도 반드시 묻는다.
